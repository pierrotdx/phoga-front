name: Build
on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  sonarqube:
    name: SonarQube
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Shallow clones should be disabled for a better relevancy of analysis
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      - name: Install dependencies
        run: npm ci
      - name: Test and coverage - "shared" project
        run: ng test:shared:ci
      - name: Test and coverage - "back-office" project
        run: ng test:back-office:ci
      - name: Test and coverage - "client" project
        run: ng test:client:ci
